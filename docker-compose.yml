version: '3.8'

services:
  rover-tracker:
    build: .
    container_name: rover-tracker
    restart: unless-stopped
    ports:
      - "127.0.0.1:3010:3000"
    volumes:
      - ./data:/app/data
      - ./uploads:/app/uploads
    env_file:
      - .env
